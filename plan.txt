🧭 Обязательный минимум функционала, чтобы считаться «mid-level IoT dashboard»:
1. Аутентификация и роли

Страница логина (фейковая авторизация через msw или mock API).

Хранение пользователя в RTK store.

Простая система ролей (например: admin, operator).

Пример: оператор видит только устройства, админ — ещё и конфигурацию сети.

2. Главная (Dashboard)

Виджет со сводкой: количество устройств, активных тревог, онлайн/оффлайн.

Карточки или таблицы со статусом устройств.

Мини-графики (температура, уровень сигнала, напряжение и т.д.)

В идеале — автообновление (polling через RTK Query refetchInterval).

3. Страница устройств (Devices)

Таблица со списком устройств:

ID, имя, тип, статус (online/offline), последнее сообщение.

Возможность кликнуть → перейти на детальную страницу.

Фильтры и поиск (по имени, типу, статусу).

Иконки состояний (например, 🟢/🔴 для статуса).

4. Страница устройства (Device Details)

История телеметрии (line chart: температура, влажность, ток и т.п.)

История тревог/уведомлений.

Настройки устройства (например, “порог тревоги”, “интервал опроса”).

Кнопка “отправить команду” (фейковый POST через RTKQ mutation).

5. Alarm/Notification система

Список последних тревог (как твой AlarmList).

Состояния: active, fixed, acknowledged.

Цветовые статусы (красный, жёлтый, серый).

Возможность “пометить исправленным”.

В идеале — веб-сокет или polling имитация (через msw).

6. Аналитика (Analytics page)

Графики по выбранному периоду (Recharts, Chart.js или что-то простое).

Возможность выбрать интервал: “час”, “день”, “неделя”.

Сравнение нескольких сенсоров.

Может быть heatmap или timeline тревог.

7. Настройки пользователя

Тема (light/dark).

Переключение языка (ru/en).

(Для IoT — можно добавить timezone или формат даты).

8. 404 / Fallback / ErrorBoundary

Если нет данных / нет соединения — показывать соответствующие состояния.

Глобальный обработчик ошибок RTKQ + ErrorBoundary на уровне Layout.


--------------------


можно еще потестить или подумать над тем, чтобы добавить инфити добавление уведомлени типа в какой то вкладке как у chirpstack
